import { BookOpen, Brain, CheckCircle, FileText, Upload, Sparkles, X, ChevronRight, HelpCircle, RefreshCw, Trophy, Home } from 'lucide-react';

export const ICONS = {
  Book: BookOpen,
  Brain: Brain,
  Check: CheckCircle,
  File: FileText,
  Upload: Upload,
  Sparkles: Sparkles,
  Close: X,
  Next: ChevronRight,
  Hint: HelpCircle,
  Refresh: RefreshCw,
  Trophy: Trophy,
  Home: Home
};

export const MAX_FILES = 10;
export const ALLOWED_TYPES = ['application/pdf', 'text/plain', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'image/png', 'image/jpeg', 'image/webp'];
